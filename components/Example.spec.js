import {shallowMount} from '@vue/test-utils';

describe('Example component', () => {
  it('should render the message', () => {
    expect(true).to.be.true;
  });

  it('should have a value of true for error if .error is present', () => {
    expect(true).to.be.true;
  });

  it('should have a value of false for error if .error is not present', () => {
    expect(true).to.be.true;
  })
});
